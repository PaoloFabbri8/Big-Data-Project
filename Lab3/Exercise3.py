# Exercise3 Sales and aggreagtion
# Read products.json in a dataframe.
# Read sales.csv in dataframe.
# Join the data using product_id as a key.
# Calculate the total sales for each product (e.g., total_sales = quantity * price).
# Remove all the sales that are less than 300.
# Write the combined data, including total sales, to a new CSV file (aggregated_sales.csv).
# There is any unknown item that you can not handle? can you export their ids in a dedicated file
# Calculate the sales for each category, visualize them with a plot
# Which category had the most sales?
# Which category sold the least?

